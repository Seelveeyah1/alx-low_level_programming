#include <stdio.h>
/**
 * main - to print the alphabet in reverse lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

char k;
for (k = 'z'; k >= 'a'; k++)
{
	putchar(k);
}
	putchar('\n');
return (0);
}
